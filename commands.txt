cd /root/data/My_first_project/TLM

HF_HOME=/root/data/qsh/hf_cache
LR=0.00014
# Based on the pulled A800 OOM log and the active dataset profiles:
# - agriculture mixed40 at 1536 tokens already OOMed at train batch size 32
# - alpaca_gpt4_5k_advharm_40 now uses the active long-form mixed40 profile (2048 / 512)
# - a safer full-suite starting point is TRAIN_BS=16, EVAL_BS=16
TRAIN_BS=16
EVAL_BS=16
GA_STEPS=1
SAVE_STEPS=60
SEED=42
NUM_WORKERS=8
MODEL_NAME="Qwen/Qwen2.5-7B-Instruct"
OUTPUT_ROOT="saves/serial_suites/requested_suite"
SWANLAB_PROJECT="TLM_new_A800"
SWANLAB_WORKSPACE="qding666"

mkdir -p logs

# Latest code enables dataset profiles by default.
# Do not add --disable-dataset-profiles unless you intentionally want the old uniform behavior.
# This is the full requested serial suite.
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True nohup python scripts/experiments/run_requested_ttl_serial_suite.py \
  --model-name-or-path "$MODEL_NAME" \
  --template qwen \
  --dataset-dir data \
  --hf-home "$HF_HOME" \
  --output-root "$OUTPUT_ROOT" \
  --learning-rate "$LR" \
  --per-device-train-batch-size "$TRAIN_BS" \
  --per-device-eval-batch-size "$EVAL_BS" \
  --gradient-accumulation-steps "$GA_STEPS" \
  --save-steps "$SAVE_STEPS" \
  --preprocessing-num-workers "$NUM_WORKERS" \
  --seed "$SEED" \
  --skip-export \
  --skip-upload \
  --use-swanlab \
  --swanlab-project "$SWANLAB_PROJECT" \
  --swanlab-workspace "$SWANLAB_WORKSPACE" \
  --swanlab-mode cloud \
  > "logs/requested_suite_lr${LR}_trainbs${TRAIN_BS}_evalbs${EVAL_BS}_seed${SEED}.log" \
  2> "logs/requested_suite_lr${LR}_trainbs${TRAIN_BS}_evalbs${EVAL_BS}_seed${SEED}.err" &


# Agriculture-only formal rerun on latest code.
# Uses the new --only-dataset entry and keeps dataset profiles enabled.
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True nohup python scripts/experiments/run_requested_ttl_serial_suite.py \
  --only-dataset agriculture_5k \
  --model-name-or-path "$MODEL_NAME" \
  --template qwen \
  --dataset-dir data \
  --hf-home "$HF_HOME" \
  --output-root "$OUTPUT_ROOT" \
  --learning-rate "$LR" \
  --per-device-train-batch-size "$TRAIN_BS" \
  --per-device-eval-batch-size "$EVAL_BS" \
  --gradient-accumulation-steps "$GA_STEPS" \
  --save-steps "$SAVE_STEPS" \
  --preprocessing-num-workers "$NUM_WORKERS" \
  --seed "$SEED" \
  --skip-export \
  --skip-upload \
  --use-swanlab \
  --swanlab-project "$SWANLAB_PROJECT" \
  --swanlab-workspace "$SWANLAB_WORKSPACE" \
  --swanlab-mode cloud \
  > "logs/agriculture_only_lr${LR}_trainbs${TRAIN_BS}_evalbs${EVAL_BS}_seed${SEED}.log" \
  2> "logs/agriculture_only_lr${LR}_trainbs${TRAIN_BS}_evalbs${EVAL_BS}_seed${SEED}.err" &


# Resume from the agriculture mixed40 OOM point.
# This skips the already-completed agriculture base eval + clean pipeline,
# reruns agriculture mixed40, then continues with alpaca_gpt4_5k and gsm8k_5k.
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True nohup python scripts/experiments/run_requested_ttl_serial_suite.py \
  --start-from-dataset agriculture_5k \
  --resume-from-step mixed_pipeline \
  --model-name-or-path "$MODEL_NAME" \
  --template qwen \
  --dataset-dir data \
  --hf-home "$HF_HOME" \
  --output-root "$OUTPUT_ROOT" \
  --learning-rate "$LR" \
  --per-device-train-batch-size "$TRAIN_BS" \
  --per-device-eval-batch-size "$EVAL_BS" \
  --gradient-accumulation-steps "$GA_STEPS" \
  --save-steps "$SAVE_STEPS" \
  --preprocessing-num-workers "$NUM_WORKERS" \
  --seed "$SEED" \
  --skip-export \
  --skip-upload \
  --use-swanlab \
  --swanlab-project "$SWANLAB_PROJECT" \
  --swanlab-workspace "$SWANLAB_WORKSPACE" \
  --swanlab-mode cloud \
  > "logs/requested_suite_resume_from_agriculture_mixed_lr${LR}_trainbs${TRAIN_BS}_evalbs${EVAL_BS}_seed${SEED}.log" \
  2> "logs/requested_suite_resume_from_agriculture_mixed_lr${LR}_trainbs${TRAIN_BS}_evalbs${EVAL_BS}_seed${SEED}.err" &


# =========================
# Windows PowerShell smoke
# =========================
# Run this from the repository root in an activated Windows `TLM` environment.
# Smoke mode will:
# 1. really export merged models to `exported_model`
# 2. run the ModelScope upload step in dry-run mode by default
#    so the tiny smoke model will NOT actually be pushed.

Set-Location .\TLM

$env:HF_HOME = "D:\hf_cache"
$env:PYTORCH_CUDA_ALLOC_CONF = "expandable_segments:True"

# Optional: only needed if you also want SwanLab online logging on Windows.
# $env:SWANLAB_API_KEY = "your_swanlab_api_key"

# Optional: not required for smoke dry-run upload, but useful for later real uploads.
# $env:MODELSCOPE_API_TOKEN = "your_modelscope_token"

$LR = "0.0001"
$TRAIN_BS = "1"
$EVAL_BS = "1"
$GA_STEPS = "1"
$SEED = "42"
$NUM_WORKERS = "1"
$MODEL_NAME = "Qwen/Qwen2.5-7B-Instruct"
$OUTPUT_ROOT = "saves/serial_suites/requested_suite_smoke"
$SWANLAB_PROJECT = "TLM_windows_smoke"
$SWANLAB_WORKSPACE = "qding666"

New-Item -ItemType Directory -Force logs | Out-Null

python scripts/experiments/run_requested_ttl_serial_suite.py `
  --smoke-test `
  --model-name-or-path $MODEL_NAME `
  --hf-home $env:HF_HOME `
  --output-root $OUTPUT_ROOT `
  --per-device-train-batch-size $TRAIN_BS `
  --per-device-eval-batch-size $EVAL_BS `
  --gradient-accumulation-steps $GA_STEPS `
  --preprocessing-num-workers $NUM_WORKERS `
  --seed $SEED `
  --use-swanlab `
  --swanlab-project $SWANLAB_PROJECT `
  --swanlab-workspace $SWANLAB_WORKSPACE `
  --swanlab-mode cloud `
  *>&1 | Tee-Object -FilePath "logs\requested_suite_smoke_export_upload.log"
