cd /root/data/My_first_project/TLM

HF_HOME=/root/data/qsh/hf_cache
LR=0.0001
TRAIN_BS=16
EVAL_BS=16
GA_STEPS=1
SEED=42
NUM_WORKERS=8
MODEL_NAME="Qwen/Qwen2.5-7B-Instruct"
OUTPUT_ROOT="saves/serial_suites/requested_suite"
SWANLAB_PROJECT="TLM_new_A800"
SWANLAB_WORKSPACE="qding666"

mkdir -p logs

# Latest code enables dataset profiles by default.
# Do not add --disable-dataset-profiles unless you intentionally want the old uniform behavior.
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True nohup python scripts/experiments/run_requested_ttl_serial_suite.py \
  --model-name-or-path "$MODEL_NAME" \
  --template qwen \
  --dataset-dir data \
  --hf-home "$HF_HOME" \
  --output-root "$OUTPUT_ROOT" \
  --learning-rate "$LR" \
  --per-device-train-batch-size "$TRAIN_BS" \
  --per-device-eval-batch-size "$EVAL_BS" \
  --gradient-accumulation-steps "$GA_STEPS" \
  --preprocessing-num-workers "$NUM_WORKERS" \
  --seed "$SEED" \
  --skip-export \
  --skip-upload \
  --use-swanlab \
  --swanlab-project "$SWANLAB_PROJECT" \
  --swanlab-workspace "$SWANLAB_WORKSPACE" \
  --swanlab-mode cloud \
  > "logs/requested_suite_lr${LR}_trainbs${TRAIN_BS}_evalbs${EVAL_BS}_seed${SEED}.log" \
  2> "logs/requested_suite_lr${LR}_trainbs${TRAIN_BS}_evalbs${EVAL_BS}_seed${SEED}.err" &


# =========================
# Windows PowerShell smoke
# =========================
# Run this from the repository root in an activated Windows `TLM` environment.
# Smoke mode will:
# 1. really export merged models to `exported_model`
# 2. run the ModelScope upload step in dry-run mode by default
#    so the tiny smoke model will NOT actually be pushed.

Set-Location .\TLM

$env:HF_HOME = "D:\hf_cache"
$env:PYTORCH_CUDA_ALLOC_CONF = "expandable_segments:True"

# Optional: only needed if you also want SwanLab online logging on Windows.
# $env:SWANLAB_API_KEY = "your_swanlab_api_key"

# Optional: not required for smoke dry-run upload, but useful for later real uploads.
# $env:MODELSCOPE_API_TOKEN = "your_modelscope_token"

$LR = "0.0001"
$TRAIN_BS = "1"
$EVAL_BS = "1"
$GA_STEPS = "1"
$SEED = "42"
$NUM_WORKERS = "1"
$MODEL_NAME = "Qwen/Qwen2.5-7B-Instruct"
$OUTPUT_ROOT = "saves/serial_suites/requested_suite_smoke"
$SWANLAB_PROJECT = "TLM_windows_smoke"
$SWANLAB_WORKSPACE = "qding666"

New-Item -ItemType Directory -Force logs | Out-Null

python scripts/experiments/run_requested_ttl_serial_suite.py `
  --smoke-test `
  --model-name-or-path $MODEL_NAME `
  --hf-home $env:HF_HOME `
  --output-root $OUTPUT_ROOT `
  --per-device-train-batch-size $TRAIN_BS `
  --per-device-eval-batch-size $EVAL_BS `
  --gradient-accumulation-steps $GA_STEPS `
  --preprocessing-num-workers $NUM_WORKERS `
  --seed $SEED `
  --use-swanlab `
  --swanlab-project $SWANLAB_PROJECT `
  --swanlab-workspace $SWANLAB_WORKSPACE `
  --swanlab-mode cloud `
  *>&1 | Tee-Object -FilePath "logs\requested_suite_smoke_export_upload.log"
